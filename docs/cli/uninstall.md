---
summary: "CLI reference for `skynet uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `skynet uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
skynet uninstall
skynet uninstall --all --yes
skynet uninstall --dry-run
```
